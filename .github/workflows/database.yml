name: database

on: 
  push: 
    branches:
      - master 

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v6

      - name: connection
        run: pytest ./testing/integration_testing/connection_testing.py
